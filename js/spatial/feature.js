/**
 * Represents a single spatial feature.
 *
 * A Feature is immutable domain data describing something that exists
 * in the world. It deliberately contains no rendering or application logic.
 *
 * At this stage the implementation is intentionally empty.
 * The purpose of this class is to establish the first domain concept.
 */
export class Feature {

}